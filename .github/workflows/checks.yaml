# useful comments
name: checks

on:
  push:
  pull_request:
    branches: [ $default-branch ]


jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
    - name: Check out the code
      uses: actions/checkout@v3
    # todo add
    # pylint
    # black
